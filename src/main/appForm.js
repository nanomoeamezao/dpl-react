import React, { Component } from "react";
import { Button, Table, Container } from "reactstrap";
import { socket } from "../global/header";

class AppForm extends Component{
    constructor(){
        super();
    }
    render() {
        return (
            <Container>
                <p> teest return for appForm</p>
            </Container>
        );
    }
}
export default AppForm;